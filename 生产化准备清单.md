# 生产化准备清单（Git 提交前）

> 结论：**需要做**。标准生产仓库在 git 提交前应补齐一批文档/配置，否则代码"无归档、无说明、无入门路径"。
> 本清单基于 `memory-system/` 实际目录核对（2026-08-16），标注了**已有 / 缺失 / 待修正**。

---

## 一、现状核对

### ✅ 已有
| 文件 | 状态 |
|---|---|
| `ARCHITECTURE.md` | 竣工架构文档（11 节，已含缺口清单） |
| `性能基线与容量规划.md` | 实测性能/一致性/压力分档 |
| `阶段总结-分层记忆系统架构.md` | 阶段反思（关键决策/踩坑） |
| `requirements.txt` | **存在但有缺陷，见下方修正** |
| `docker-compose.yml` | PG16+pgvector + Redis 编排 |
| `conftest.py` | 测试统一清理 |

### ❌ 缺失（git 前必补）
README / 架构图 / FAQ / .gitignore / LICENSE / .env.example / pytest 配置 / CHANGELOG

### ⚠️ 待修正
- `requirements.txt`：`requests== 2.32.5` **多了空格**（无法安装）；`pip==21.2.4` 不应锁版本；**缺 `python-dotenv`**（`summary_api/vector_semantic/rag_knowledge` 都 `from dotenv import load_dotenv`）
- 根目录 `1.py`、`embedding_test.py`、`insert_*.py` 是临时脚本，需归类或标注
- `终端pythonEOF脚本集.md` 是零散命令记录，建议拆解进 README/FAQ

---

## 二、标准生产文件清单（按优先级）

### P0 —— git 提交前必做

| 文件 | 用途 | 内容要点 |
|---|---|---|
| **README.md** | 项目门面：让人 10 分钟上手 | 项目定位、六类记忆存储、5 层检索优先级、快速启动（docker compose up + 环境变量 + 测试）、目录结构、技术栈 |
| **架构图** | 可视化（并入 ARCHITECTURE.md 或单独） | Mermaid 图：事件流总线 → 6 类存储 + 5 层检索链路 + 多租户隔离标注 |
| **.gitignore** | 防止提交垃圾文件 | `__pycache__/`、`*.pyc`、`memory-system-venv/`、`.pytest_cache/`、`data/db/`、`perf_baselines.json`（可选保留）、`skills/user_test_*/` |
| **.env.example** | 环境变量模板 | `DEEPSEEK_API_KEY=`、`OLLAMA_URL=http://localhost:11434`、PG/Redis 连接参数（对齐 docker-compose） |
| **LICENSE** | 开源/使用权声明 | 若公开选 MIT/Apache-2.0；纯学习可注明"学习用途" |
| **requirements.txt（修正）** | 可复现依赖 | 去空格、去 pip 锁、补 `python-dotenv`，用 `pip freeze` 重新生成 |
| **pytest.ini 或 pyproject.toml** | pytest 配置 | `testpaths = test_*.py`、可选 `-p no:cacheprovider`、addopts |
| **CHANGELOG.md** | 变更历史 | 按日期记录：多租户重构 / KV schema 修复 / 技能隔离 / 测试体系（第 17-19 天成果） |

### P1 —— 强烈推荐

| 文件 | 用途 | 内容要点 |
|---|---|---|
| **FAQ.md** | 常见问题与排查 | 启动失败（PG/Redis 未起）、Ollama 未在线（embedding 失败）、Append-Only 触发器与清理、缓存串问题、多租户 user_id 约定 |
| **TEST_REPORT.md** | 测试报告 | 26 用例分布、运行命令、性能基线摘要、JSON 归档位置 |
| **data/schema.sql 或 init 脚本** | 数据库初始化 | 各表 DDL、索引、`ledger_append_only_trigger`、`user_profile` 唯一约束 `(user_id, key)` |
| **docs/ 目录重组**（可选） | 文档归类 | 把根目录 .md 归入 `docs/`：architecture / performance / faq / stage-summary |

### P2 —— 可选

| 文件 | 用途 |
|---|---|
| CONTRIBUTING.md | 协作规范（单人学习可省） |
| Makefile / run.sh | 一键启动/测试命令 |
| SECURITY.md | 安全声明（含多租户隔离说明、触发器防护） |
| CI（GitHub Actions） | 自动跑 pytest + lint（公开仓库才需要） |
| API 文档 | 若对外暴露 `unified_api` 接口 |

---

## 三、建议执行顺序

```
1. 修正 requirements.txt（当前无法装包）
2. .gitignore（先建，防误提交）
3. README.md + 架构图（门面）
4. .env.example + pytest 配置 + CHANGELOG
5. LICENSE
6. git init → 分组提交
7. FAQ / TEST_REPORT（P1，可在 git 后补）
```

---

## 四、架构图参考（Mermaid，可粘贴进 README/ARCHITECTURE）

```mermaid
flowchart LR
    subgraph 入口
        A[unified_api.record_event] --> B[event_bus.EventBus]
        C[unified_api.query_memory] --> D[retrieval_orchestrator.unified_search]
    end

    B --> L[ledger 账本<br/>Append-Only 触发器]
    B --> R[redis 热缓存<br/>kv/tool/skill:{user_id}:...]

    D --> E1[1 KV 精准]
    D --> E2[2 Summary 摘要]
    D --> E3[3 Ledger 回溯]
    D --> E4[4 Skills 复用<br/>skills/{user_id}/]
    D --> E5[5 RAG 条件性<br/>tenant_id 隔离]

    E1 --> K[(user_profile)]
    E2 --> S[(summary)]
    E3 --> L
    E4 --> P[(skill_candidate_vectors<br/>skill_snapshots)]
    E5 --> G[(rag_knowledge_base)]

    B --成功tool/skill--> P
```

---

## 五、后续动作建议

- 本清单本身可先提交，作为"待办追踪"
- 生成的 README/FAQ 等文档建议与代码同仓库（仓库根 `ARCHITECTURE.md` 模式）
- 当前目录仍**非 git 仓库**，确认后可先 `git init` 再按 P0 顺序补齐
