# 性能基线与容量规划

> 测试文件：`test_day19_performance_stability.py`
> 数据归档：`perf_baselines.json`（每次运行追加一条，保留历史便于对比）
> 测试环境：本地 macOS（PostgreSQL + Redis + Ollama bge-m3 embedding）
> 测量方式：每层独立执行 5 次，取平均；KV/Summary/Ledger 为纯 DB/缓存查询，Vector/RAG 含本地 Ollama embedding 调用

---

## 一、各层平均响应时间基线（本地环境，实测）

| 层 | 实测平均响应时间 | 影响因素 |
|---|---|---|
| KV（Cache-Aside） | **0.45 ms** | Redis 命中率、网络延迟；命中缓存时零 DB 访问 |
| Summary（PostgreSQL） | **1.28 ms** | 索引、数据量、连接池大小；滚动摘要数量 |
| Ledger（PostgreSQL） | **1.13 ms** | `user_id`+`entity_id` 索引、数据量、连接池大小 |
| Vector（pgvector + Ollama） | **260.3 ms** | **Ollama embedding 占绝对大头**；向量维度（1024）、索引类型 |
| RAG（外部知识库） | **433.8 ms** | Ollama embedding + BM25 分词 + RRF 融合 + 外部服务延迟 |

### 测量原始数据（5 次耗时，单位秒）

```
KV:      [0.000505, 0.000490, 0.000378, 0.000461, 0.000438]
Summary: [0.001986, 0.001044, 0.000974, 0.001038, 0.001352]
Ledger:  [0.001432, 0.001085, 0.001058, 0.001027, 0.001050]
Vector:  [0.260033, 0.256567, 0.254578, 0.277348, 0.253047]
RAG:     [1.020900, 0.275099, 0.343717, 0.262174, 0.267154]
```

### 排序验证结论

- **KV < Ledger < Vector < RAG**（断言通过）
- Vector/RAG 的耗时被本地 Ollama embedding 主导，**不等于数据库性能**；纯 DB 部分与 Ledger 同量级
- 阈值设定：KV/Summary/Ledger < 0.5s，Vector/RAG < 5s（为 embedding 放宽）

---

## 二、容量规划：多租户并发压力分档

> 三档压力，全部通过；每档每个用户每次查询均返回自己的数据，零串扰、零错误。

| 压力档 | 并发查询数 | 线程数 | 耗时 | 错误 |
|---|---|---|---|---|
| low | 20 | 5 | 0.030s | 0 |
| medium | 50 | 10 | 0.046s | 0 |
| high | 80 | 12 | 0.070s | 0 |

### 结论

- 连接池（`SimpleConnectionPool`，min 5 / max 20）在 12 并发线程下稳定，无连接耗尽
- 隔离性：同一 `entity_id="location"` 下 8 个用户各写各的 city，并发读取**互不串扰**
- 单次 `get_current_state` 查询约 1ms，80 次并发总耗时 0.07s → **吞吐量约 1140 查询/秒**
- 说明：生产环境压力应结合真实并发模型（多实例、连接池上限、Redis 单点）另行压测

---

## 三、一致性验证核心维度

| 维度 | 验证内容 | 覆盖层 |
|---|---|---|
| 事件存在性 | 同一事件在所有层都有记录 | Ledger / Redis 缓存 / 向量 |
| 数据内容一致 | 工具调用结果、状态变更在各层一致 | event_data / new_value / 缓存值 / 向量内容 |
| 时间戳一致 | 各层记录的时间戳偏差在合理范围内 | Ledger `timestamp` ↔ 向量 `metadata.timestamp` |
| 用户隔离 | 所有层都按 `user_id` 隔离 | 全部层（Ledger/KV/Summary/向量/RAG） |

### tool_call 一致性（实测）

```
Ledger 事件:   event_data={'tool_name':'web_search','input':'搜索天气','output':'晴天','status':'success'} new_value={'result':'晴天'}
Redis 缓存:    tool:user_test_consist:consist_entity:web_search → 同上 event_data
Vector 检索:   "工具调用 web_search：输入 搜索天气，输出 晴天"  相似度 0.8368
```

### skill_call 一致性（实测 + 缺口标注）

```
Ledger 事件:   {'skill_name':'flight_booking','trigger':'用户要求订票','output':'预订成功','status':'success'}
Redis 缓存:    skill:user_test_skillconsist:skill_consist_entity:flight_booking → 同上
Vector 检索:   命中（经显式 sync_new_events）
Summary:       skill_call_pattern = 'flight_booking'（经显式 save_summary）
```

**已知缺口**：`event_bus.record_event` 对 tool_call / skill_call **不会自动写入向量库，也不会自动触发摘要更新**——这两层的一致性依赖显式同步流程（`sync_new_events` / `update_summary`）。如需生产级自动传播，需在 `event_bus` 增加对应逻辑。

---

## 四、RAG 版本同步（条件性调用）

| 阶段 | 动作 | 结果 |
|---|---|---|
| 写入 v1 | `add_external_knowledge(version="v1")` | 状态 active |
| 首次条件检索 | `rag_conditional_search` | `snapshot_updated=True`，快照记录 v1 |
| 写入 v2 | 同 source+title 自动废弃 v1 | v1 → deprecated，v2 → active |
| 再次检索 | 检测到新版本 | `snapshot_updated=True`，快照更新为 v2 |
| 最终检索 | `search_rag_knowledge` | 只返回 active（v2），v1 不再返回 |

### RAG 条件性判定矩阵

| 条件 | 是否调用 RAG |
|---|---|
| 内部完整命中 + 无引用 + 无外部关键词 | ✗ 不调用 |
| 内部完全未命中（需外挂知识） | ✓ 调用 |
| 内部命中但 query 含外部知识关键词（最新/文档/API 规范…） | ✓ 调用 |
| 有 `rag_reference` 引用标记 | ✓ 调用 |

---

## 五、程序性记忆进化链路（补充验证）

> 测试文件：`test_day18_evolution_conditional.py`

| 阶段 | 函数 | 说明 |
|---|---|---|
| 记录 | `add_candidate_vector` | 重复成功任务写入候选（按 user_id 隔离） |
| 触发 | `check_procedural_trigger` | 双阈值判定（success 相似 + query 相似） |
| 封装 | `save_skill_to_skills_dir` | 写 `skills/{user_id}/` SKILL.md |
| 固化 | `finalize_skill` | 写 skill_snapshots 快照 + 标记候选已处理 |
| 复用 | `match_skill_by_description` | 命中已固化技能，**跳过昂贵推理**（生成函数未被调用） |

---

## 六、测试命令与归档

```bash
# 完整运行 3 个测试文件（含性能）
python -m pytest test_day17_deterministic_backtrack.py \
                 test_day18_evolution_conditional.py \
                 test_day19_performance_stability.py -q

# 仅运行性能与稳定性测试（-s 显示原始耗时）
python -m pytest test_day19_performance_stability.py -s

# 基线 JSON 归档（perf_baselines.json，追加式）
# 结构：performance_baseline（各层 avg + raw）+ multi_tenant_concurrency（压力分档）
```

### 归档 JSON 结构示例

```json
{
  "type": "performance_baseline",
  "timestamp": "2026-08-16T13:51:03+00:00",
  "thresholds_seconds": {"KV": 0.5, "Vector": 5.0, "RAG": 5.0},
  "layers_avg_seconds": {"KV": 0.000454, "Ledger": 0.001131, "Vector": 0.260315, "RAG": 0.433809},
  "layers_raw_seconds": {"KV": [...], "RAG": [...]}
}
```

---

## 七、测试总览（26 个用例全绿）

| 测试文件 | 用例数 | 覆盖 |
|---|---|---|
| `test_day17_deterministic_backtrack.py` | 13 | 确定性、时间回溯、KV 零噪声、多租户隔离、Append-Only、参数化隔离、时间范围 |
| `test_day18_evolution_conditional.py` | 8 | 程序性记忆进化链路、RAG 条件性、RAG 版本对比、技能匹配隔离、description 匹配、参数化不串扰 |
| `test_day19_performance_stability.py` | 5 | 性能基线、一致性、skill_call 一致性、RAG 版本同步、并发压力分档 |

**核心结论**：本地单机环境下，纯 DB/缓存层毫秒级、向量/RAG 层受 embedding 主导为百毫秒级；多租户隔离在全部层生效且并发稳定；基线数据已 JSON 归档可持续对比。
