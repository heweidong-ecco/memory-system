# 阶段总结：分层记忆系统架构（20 天）

> 完成日期：2026-08-16（本阶段代码 + 测试全部落地）
> 归档路径：Obsidian 项目/分层记忆系统架构/
> 关联文档：`ARCHITECTURE.md`（竣工文档）、`性能基线与容量规划.md`、`perf_baselines.json`

---

## 1. 关键决策记录

| 决策 | 理由 | 影响 |
|---|---|---|
| 分层记忆系统替代 Mem0 + Obsidian Bridge | 需要生产级多租户隔离与自进化能力 | 学习周期 10 天→20 天 |
| 程序性记忆 = Agent 自我进化产物 | 从自己成功经历提炼，不是外部 Skills | 架构核心 |
| RAG 条件性调用（4 种触发条件） | 外部知识不默认调用，避免浪费 | 检索优先级最后 |
| 多租户 user_id 全链路透传 | 所有表/查询/缓存键按用户隔离 | 第 17-19 天大规模重构 |
| 技能文件按用户私有目录 `skills/{user_id}/` | 用户生成的技能不跨租户泄漏 | Phase 2 重构 |
| 公共 RAG 用 `tenant_id` 区分 | 单一数据源，多 Agent 共享 | 产品阶段拆独立服务 |

---

## 2. 踩坑教训

| 坑 | 教训 |
|---|---|
| user_id 迁移遗漏调用点 → 崩溃 | `get_entity_history`/`get_events_by_session`/`kv_key` 改签名后，旧调用点未同步更新（TypeError）；**签名变更必须全局 grep 调用方** |
| `user_profile` 主键只有 `key` | 两用户写同 key 在 DB 层互相覆盖，**缓存掩盖了隔离失效**；需 `PRIMARY KEY (user_id, key)` |
| Append-Only 触发器 ↔ 测试清理冲突 | 测试清理需临时 `DISABLE TRIGGER` 后 DELETE 再恢复；**生产严禁** |
| 多租户缓存键未含 user_id | 同 entity 跨用户缓存串；键格式必须含隔离维度 `kv:{user_id}:{entity_id}:{field}` |
| 测试文件引用不存在的函数/表 | `maybe_trigger_extraction`/`find_best_skill`/`get_rag_version_snapshot`/`rag_metadata` 均为虚构；**测试必须先核验真实 API** |
| tool_call/skill_call 不自动传播向量/Summary | `event_bus` 只写 Ledger + Redis 缓存；向量/摘要需显式同步——**事件传播需全局设计，不是只写一处** |

---

## 3. 实测性能基线（2026-08-16）

| 层 | 平均 | 说明 |
|---|---|---|
| KV | 0.45 ms | 缓存命中零 DB |
| Summary | 1.28 ms | 纯 PG |
| Ledger | 1.13 ms | 索引优化后 |
| 向量 | 260 ms | Ollama embedding 主导 |
| RAG | 434 ms | embedding + 融合 |

并发：low 20 / medium 50 / high 80 查询，0.03/0.05/0.07s，零错误。

---

## 4. 下一步行动

1. 进入阶段 7：本地 AI 工作站 + VM + 备份策略（2026-08-19 开始）
2. 产品阶段将公共 RAG 拆分为独立服务（REST API/MCP）
3. 持续优化：向量索引 HNSW、程序性记忆自动触发机制、`TTL_RAG` 落地、`ThreadedConnectionPool`
4. 补齐遗留缺口：tool_call/skill_call 自动传播到向量层与 Summary

---

## 5. 收工清单

- [x] 架构文档 `ARCHITECTURE.md` 已完成
- [x] 性能基线文档 + JSON 归档已生成
- [ ] Git 提交（⚠️ **当前目录非 git 仓库，需先 `git init`**）
- [ ] 学习路线图更新（标记本阶段完成，日期修正为 2026-08-16）
- [ ] Obsidian 归档（复制上述 .md 到 vault 项目/分层记忆系统架构/）
- [ ] 能向别人清晰解释 5 层检索优先级 + 6 类记忆存储 + 多租户设计
